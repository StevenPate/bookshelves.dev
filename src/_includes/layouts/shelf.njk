{% extends 'layouts/base.njk' %}

{% block content %}
<article>
<section title="About the '{{ title }}' shelf" class="prose prose-xl">
  <article>
  <h1>{{ title or metadata.title }}</h1>
  {{ content | safe }}
  </article>
</section>
<section>
  {% for book in books %}
    {% _shortcode book.ISBN, "full", "", "purchase", "", page.fileSlug %}
    {# <div>{% _shortcode "book.ISBN, "full", "samplewords", "purchase"" %}</div> #}
    {# {% book book.ISBN, 'full', '', 'purchase', '', page.inputPath %} #}
    {# {% _shortcode book.ISBN, 'full', '', 'purchase', '', page.inputPath %} #}
  {% endfor %}
</section>
</article>
{% endblock %}
